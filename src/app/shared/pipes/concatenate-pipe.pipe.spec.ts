import { ConcatenatePipePipe } from './concatenate-pipe.pipe';

describe('ConcatenatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatenatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
